This is a simple example of blinking an led and printing hello world in a loop.

```
nix develop
cargo run --release
```

It was tested using an ESP-WROOM-32 with an `LED connected at Pin 23`.

![wiring](./diagram.png?raw=true)
